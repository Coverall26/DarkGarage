import GPDashboardClient from "./page-client";

export const metadata = {
  title: "Dashboard — FundRoom | FundRoom AI",
};

export default function GPDashboardPage() {
  return <GPDashboardClient />;
}
