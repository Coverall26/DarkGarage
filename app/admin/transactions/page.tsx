import TransactionsClient from "./page-client";

export const metadata = {
  title: "Transactions — Raise Dashboard | FundRoom AI",
};

export default function TransactionsPage() {
  return <TransactionsClient />;
}
